import React from 'react';
import ReactDOM from 'react-dom';
import MochiMap from './MochiMap';

ReactDOM.render(
  <React.StrictMode>
    <MochiMap />
  </React.StrictMode>,
  document.getElementById('root')
);
